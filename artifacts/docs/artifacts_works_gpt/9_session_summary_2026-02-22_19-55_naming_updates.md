# Session Summary — Naming Updates (artifacts_works_gpt)

Дата фиксации: 2026-02-22

## Что изменено

1. Принято и зафиксировано правило именования файлов в папке [`artifacts/docs/artifacts_works_gpt`](.):
- в имя файла добавляются дата и время;
- в начале имени добавляется сквозная нумерация (`1_`, `2_`, `3_`, ...).

2. Для существующих файлов в [`artifacts/docs/artifacts_works_gpt`](.) добавлено время в имени:
- время взято из `mtime` (время последнего изменения файла).

3. Для существующих файлов добавлена нумерация в начале имени:
- порядок нумерации выполнен хронологически (от более старых к более новым файлам).

## Итоговое состояние

- Имена файлов в папке приведены к единому формату с префиксной нумерацией и временной меткой.
- Последующие session-summary в этой папке должны создаваться с тем же правилом.
