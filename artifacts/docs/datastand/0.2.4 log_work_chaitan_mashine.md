# Лог работ по развёртыванию стенда

Дата: 2026-02-16
Проект: `portfoilo`

## 1) Что было проанализировано

- Проанализированы документы в [`artifacts/docs/`](..):
  - `0.1 Инфра-спек_чек_лист_развернутые требования к инфре.md`
  - `0.2 Чеклист.md`
  - `0.2.2. Compose базовый каркас.md`
  - `0.2.3.2 Сервис db-init one-shot (profile db-init).md`
  - `4.1 Docker postgresql airflow superset.md`
- Сверена фактическая реализация с требованиями: `compose.yml`, `sql/db/*`, `docker/db-init/*`, `scripts/*`, структура [`airflow/`](../../../airflow), [`superset/`](../../../superset).

## 2) Выполненные изменения

### 2.1 Postgres / db-init

Исправлены и доведены до рабочего состояния:

- [`docker/db-init/db-init.sh`](../../../docker/db-init/db-init.sh)
  - добавлена поддержка режимов `MODE=full-init` и `MODE=reset-soft`;
  - сохранён строгий порядок инициализации;
  - реализован сценарий soft reset через отдельный SQL.

- [`sql/db/000_roles.sql`](../../../sql/db/000_roles.sql)
  - переписан на идемпотентный сценарий через `SELECT ... \gexec`;
  - устранена ошибка подстановки psql-переменных внутри `DO $$`.

- [`sql/db/050_project_baseline.sql`](../../../sql/db/050_project_baseline.sql)
  - исправлены имена psql-переменных (`PROJECT_USER`);
  - добавлены демо-таблицы для seed:
    - `mart.demo_kpi_daily`
    - `mon.demo_dag_runs_daily`

- [`sql/db/800_soft_reset.sql`](../../../sql/db/800_soft_reset.sql)
  - добавлен сценарий soft reset (drop/recreate схем `raw/stg/mart/mon`).

### 2.2 Compose и окружение

- `compose.yml`
  - расширен до полного стека:
    - `postgres` (profiles: `db`, `db-init`, `airflow`, `superset`)
    - `db-init`
    - `airflow-init`, `airflow-webserver`, `airflow-scheduler`
    - `superset-init`, `superset`
  - настроены сети, volumes, порты, env, healthcheck-и, зависимости.

- `.env.example`
  - дополнен переменными для:
    - Postgres
    - Airflow
    - Superset
    - режима `DB_INIT_MODE`

- `.gitignore`
  - приведён к корректным путям + добавлены python-кэши.

### 2.3 Airflow

Добавлены/настроены файлы:

- [`airflow/Dockerfile`](../../../airflow/Dockerfile)
- `requirements-airflow.txt`
- [`airflow/dags/demo_healthcheck_dag.py`](../../../airflow/dags/demo_healthcheck_dag.py)

Ключевые правки:

- убран конфликт зависимостей Airflow (SQLAlchemy 2.x не используется в airflow-образе);
- реализован рабочий сценарий инициализации метабазы и создания admin-пользователя в [`scripts/up-airflow.sh`](../../../scripts/up-airflow.sh);
- добавлен шаг `build` перед запуском `airflow-init` для гарантии актуального образа.

### 2.4 Superset

Добавлены/настроены файлы:

- [`superset/Dockerfile`](../../../superset/Dockerfile)
- [`superset/superset_config.py`](../../../superset/superset_config.py)
- [`superset/bootstrap.sh`](../../../superset/bootstrap.sh)

Ключевые правки:

- инициализация Superset БД + admin;
- добавлено автосоздание/обновление подключения к `project_db`:
  - `superset set-database-uri -d project_db -u "$PROJECT_DB_URI"`;
- добавлен шаг `build` перед `superset-init` в [`scripts/up-superset.sh`](../../../scripts/up-superset.sh).

### 2.5 Скрипты управления стендом

Обновлены:

- [`scripts/lib.sh`](../../../scripts/lib.sh)
  - добавлены функции работы от корня проекта (`cd_root`);
  - поддержка выбора `compose.yml`/`compose.yaml`;
  - поддержка автоподстановки `.env` / `.env.example`.

- [`scripts/up-db.sh`](../../../scripts/up-db.sh)
- [`scripts/init-db.sh`](../../../scripts/init-db.sh)
- [`scripts/reset-soft.sh`](../../../scripts/reset-soft.sh)
- [`scripts/reset-hard.sh`](../../../scripts/reset-hard.sh)
- [`scripts/up-airflow.sh`](../../../scripts/up-airflow.sh)
- [`scripts/up-superset.sh`](../../../scripts/up-superset.sh)
- [`scripts/up-all.sh`](../../../scripts/up-all.sh)
- [`scripts/smoke.sh`](../../../scripts/smoke.sh)

Сделано:

- устранена нестабильность pre-check логики сервисов;
- обеспечен запуск команд из любого рабочего каталога;
- общий smoke дополнен вызовом `db-smoke.sh` с корректным экспортом env.

### 2.6 Make/документация

- создан/обновлён корневой `Makefile` с целями:
  - `up-db`, `init-db`, `reset-soft`, `reset-hard`, `up-airflow`, `up-superset`, `up-all`, `smoke`
- обновлён [`scripts/README.md`](../../../scripts/README.md) с runbook и адресами UI.

## 3) Проверки и фактические результаты

Проведены реальные прогоны в Docker:

1. [`./scripts/up-db.sh`](../../../scripts/up-db.sh) — успешно.
2. [`./scripts/init-db.sh`](../../../scripts/init-db.sh) — успешно.
3. [`./scripts/reset-soft.sh`](../../../scripts/reset-soft.sh) — успешно.
4. [`./scripts/smoke.sh`](../../../scripts/smoke.sh) — успешно:
   - 3 БД существуют;
   - схемы `raw/stg/mart/mon` существуют;
   - `mon.applied_migrations` существует;
   - seed загружен (`30` строк в каждой демо-таблице).
5. [`./scripts/up-airflow.sh`](../../../scripts/up-airflow.sh) — успешно:
   - метабаза мигрирована;
   - admin создан;
   - webserver/scheduler в `Up`.
6. [`./scripts/up-superset.sh`](../../../scripts/up-superset.sh) — успешно:
   - миграции выполнены;
   - admin инициализирован;
   - `project_db` connection создана/обновлена;
   - сервис `superset` в `Up`.
7. [`./scripts/up-all.sh`](../../../scripts/up-all.sh) — успешно (после стабилизации скриптов).

Итоговый статус стенда:

- `postgres` — `Up (healthy)`
- `airflow-webserver` — `Up`
- `airflow-scheduler` — `Up`
- `superset` — `Up (healthy)`

## 4) Что готово для демонстрации

Готов рабочий Docker-стенд (MVP) с модульным и единым запуском:

- БД: Postgres + db-init + seed + soft/hard reset
- Оркестрация: Airflow (LocalExecutor)
- BI: Superset с подключением к проектной БД
- smoke-проверка инфраструктуры

## 5) Что осталось (следующий этап)

- Добавить и импортировать боевые Superset assets (`datasets/charts/dashboards`) в [`superset/assets/`](../../../superset/assets).
- Добавить полноценные DAG-и под сценарии портфолио в [`airflow/dags/`](../../../airflow/dags).
- Доработать документацию верхнего уровня (`README.md`) под финальную демо-подачу.
