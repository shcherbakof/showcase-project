## Что за пункт 3.2 и зачем он нужен

Тебе нужен **повторяемый** one-shot `db-init`: поднял Postgres → дождался `healthy` → **применил SQL строго по порядку** → применил **только новые миграции** (через `applied_migrations`) → (опционально) залил seed → вышел с `0`. Это ровно то, что в твоих доках уже зафиксировано как “как в проде”, а не одноразовый `docker-entrypoint-initdb.d`.

Сейчас у тебя по чек-листу закрыт `3.1 postgres`, а `3.2 db-init` — как раз следующий обязательный шаг.

## Конкретно по шагам

1. Структура файлов

- Создай:
  - [x] [`docker/db-init/db-init.sh`](../../../docker/db-init/db-init.sh)
  - [x] [`sql/db/000_roles.sql`](../../../sql/db/000_roles.sql)
  - [x] [`sql/db/010_databases.sql`](../../../sql/db/010_databases.sql)
  - [x] [`sql/db/020_extensions.sql`](../../../sql/db/020_extensions.sql)
  - [x] [`sql/db/030_project_schemas.sql`](../../../sql/db/030_project_schemas.sql)
  - [x] [`sql/db/040_project_migrations.sql`](../../../sql/db/040_project_migrations.sql)
  - [x] [`sql/db/050_project_baseline.sql`](../../../sql/db/050_project_baseline.sql)
  - [x] [`sql/db/900_seed.sql`](../../../sql/db/900_seed.sql)
  - [`sql/db/project/migrations/`](../../../sql/db/project/migrations) (пусто сейчас, сюда будут идти будущие миграции)

1. Обнови `.env.example`

- [x] Добавь имена БД/юзеров/паролей и флаг seed (см. готовое решение ниже).

3. Обнови `compose.yaml`

- [x] Добавь сервис `db-init` (profile `db-init`), который:
  - depends_on `postgres: condition: service_healthy`
  - монтирует [`./sql/db`](../../../sql/db) и [`./docker/db-init`](../../../docker/db-init)
  - запускает `bash /docker/db-init/db-init.sh`
  - `restart: "no"`

4. Прогони

- `docker compose --env-file .env.example config`
- `docker compose --profile db up -d postgres`
- `docker compose --profile db-init run --rm db-init`

5. Smoke-проверки

- Самый прагматичный вариант: отдельный скрипт [`scripts/db-smoke.sh`](../../../scripts/db-smoke.sh) (ниже) или отдельный one-shot сервис `db-smoke`.
- Проверяешь:
  - 3 БД существуют
  - схемы `raw/stg/mart/mon` есть в `project_db`
  - `mon.applied_migrations` есть
  - seed (если включён) дал строки в demo-таблицы

## Готовое решение (копируй 1-в-1)

### 1) `.env.example` (добавь)

```dotenv
# DB names
PROJECT_DB=project_db
AIRFLOW_DB=airflow_db
SUPERSET_DB=superset_db

# DB users (логины)
PROJECT_USER=project_user
AIRFLOW_USER=airflow_user
SUPERSET_USER=superset_user

# DB passwords (локальные примеры; в .env реальные)
PROJECT_PASS=project_pass
AIRFLOW_PASS=airflow_pass
SUPERSET_PASS=superset_pass

# db-init behavior
DB_SEED_ENABLED=1
```

(Остальные переменные Postgres (tag/порт/superuser/PGDATA/TZ/locale) у тебя уже заведены по модулю `3.1`.)

### 2) `compose.yaml` (добавь сервис)

```yaml
db-init:
  image: postgres:${POSTGRES_IMAGE_TAG}
  profiles: ["db-init"]
  container_name: ${COMPOSE_PROJECT_NAME}_db_init
  restart: "no"

  depends_on:
    postgres:
      condition: service_healthy

  environment:
    TZ: ${TZ}
    LANG: ${LANG}
    LC_ALL: ${LC_ALL}

    # psql connection (superuser)
    PGHOST: postgres
    PGPORT: 5432
    PGUSER: ${POSTGRES_SUPERUSER}
    PGPASSWORD: ${POSTGRES_SUPERPASS}

    # project params
    PROJECT_DB: ${PROJECT_DB}
    PROJECT_USER: ${PROJECT_USER}
    PROJECT_PASS: ${PROJECT_PASS}

    AIRFLOW_DB: ${AIRFLOW_DB}
    AIRFLOW_USER: ${AIRFLOW_USER}
    AIRFLOW_PASS: ${AIRFLOW_PASS}

    SUPERSET_DB: ${SUPERSET_DB}
    SUPERSET_USER: ${SUPERSET_USER}
    SUPERSET_PASS: ${SUPERSET_PASS}

    DB_SEED_ENABLED: ${DB_SEED_ENABLED}

  volumes:
    - ./sql/db:/sql/db:ro
    - ./docker/db-init:/docker/db-init:ro

  command: ["/bin/bash", "/docker/db-init/db-init.sh"]
```

### 3) [`docker/db-init/db-init.sh`](../../../docker/db-init/db-init.sh)

```bash
#!/usr/bin/env bash
set -euo pipefail

log() { printf '[db-init] %s\n' "$*"; }

: "${PGHOST:=postgres}"
: "${PGPORT:=5432}"
: "${PGUSER:?PGUSER is required}"
: "${PGPASSWORD:?PGPASSWORD is required}"

: "${PROJECT_DB:?PROJECT_DB is required}"
: "${PROJECT_USER:?PROJECT_USER is required}"
: "${PROJECT_PASS:?PROJECT_PASS is required}"

: "${AIRFLOW_DB:?AIRFLOW_DB is required}"
: "${AIRFLOW_USER:?AIRFLOW_USER is required}"
: "${AIRFLOW_PASS:?AIRFLOW_PASS is required}"

: "${SUPERSET_DB:?SUPERSET_DB is required}"
: "${SUPERSET_USER:?SUPERSET_USER is required}"
: "${SUPERSET_PASS:?SUPERSET_PASS is required}"

: "${DB_SEED_ENABLED:=1}"

PSQL_BASE=(psql -v ON_ERROR_STOP=1 -h "$PGHOST" -p "$PGPORT" -U "$PGUSER")

wait_pg() {
  log "waiting postgres ($PGHOST:$PGPORT) ..."
  for _ in $(seq 1 60); do
    if pg_isready -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" >/dev/null 2>&1; then
      log "postgres is ready"
      return 0
    fi
    sleep 2
  done
  log "postgres did not become ready"
  exit 1
}

run_file() {
  local db="$1"
  local file="$2"
  log "apply $(basename "$file") on db=$db"
  "${PSQL_BASE[@]}" -d "$db" \
    -v PROJECT_DB="$PROJECT_DB" -v PROJECT_USER="$PROJECT_USER" -v PROJECT_PASS="$PROJECT_PASS" \
    -v AIRFLOW_DB="$AIRFLOW_DB" -v AIRFLOW_USER="$AIRFLOW_USER" -v AIRFLOW_PASS="$AIRFLOW_PASS" \
    -v SUPERSET_DB="$SUPERSET_DB" -v SUPERSET_USER="$SUPERSET_USER" -v SUPERSET_PASS="$SUPERSET_PASS" \
    -f "$file"
}

apply_migrations_dir() {
  local db="$1"
  local dir="$2"

  if [[ ! -d "$dir" ]]; then
    log "migrations dir not found: $dir (skip)"
    return 0
  fi

  shopt -s nullglob
  local files=("$dir"/*.sql)
  shopt -u nullglob

  if (( ${#files[@]} == 0 )); then
    log "no migrations in $dir (skip)"
    return 0
  fi

  for f in "${files[@]}"; do
    local key
    key="$(basename "$f")"

    local applied
    applied="$("${PSQL_BASE[@]}" -d "$db" -Atc \
      "select 1 from mon.applied_migrations where migration_key = '$key' limit 1;" || true)"

    if [[ "$applied" == "1" ]]; then
      log "skip migration (already applied): $key"
      continue
    fi

    log "apply migration: $key"
    "${PSQL_BASE[@]}" -d "$db" -v ON_ERROR_STOP=1 <<SQL
BEGIN;
\\i '$f'
INSERT INTO mon.applied_migrations(migration_key) VALUES ('$key');
COMMIT;
SQL
  done
}

main() {
  wait_pg

  # строгий порядок base-init
  run_file "postgres" "/sql/db/000_roles.sql"
  run_file "postgres" "/sql/db/010_databases.sql"

  run_file "$PROJECT_DB" "/sql/db/020_extensions.sql"
  run_file "$PROJECT_DB" "/sql/db/030_project_schemas.sql"
  run_file "$PROJECT_DB" "/sql/db/040_project_migrations.sql"
  run_file "$PROJECT_DB" "/sql/db/050_project_baseline.sql"

  # будущие миграции (ключ = имя файла)
  apply_migrations_dir "$PROJECT_DB" "/sql/db/project/migrations"

  if [[ "$DB_SEED_ENABLED" == "1" ]]; then
    run_file "$PROJECT_DB" "/sql/db/900_seed.sql"
  else
    log "seed disabled (DB_SEED_ENABLED=$DB_SEED_ENABLED)"
  fi

  log "done"
}

main "$@"
```

### 4) [`sql/db/000_roles.sql`](../../../sql/db/000_roles.sql)

```sql
DO $$
BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = :'PROJECT_USER') THEN
    EXECUTE format('CREATE ROLE %I LOGIN PASSWORD %L', :'PROJECT_USER', :'PROJECT_PASS');
  ELSE
    EXECUTE format('ALTER ROLE %I WITH LOGIN PASSWORD %L', :'PROJECT_USER', :'PROJECT_PASS');
  END IF;

  IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = :'AIRFLOW_USER') THEN
    EXECUTE format('CREATE ROLE %I LOGIN PASSWORD %L', :'AIRFLOW_USER', :'AIRFLOW_PASS');
  ELSE
    EXECUTE format('ALTER ROLE %I WITH LOGIN PASSWORD %L', :'AIRFLOW_USER', :'AIRFLOW_PASS');
  END IF;

  IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = :'SUPERSET_USER') THEN
    EXECUTE format('CREATE ROLE %I LOGIN PASSWORD %L', :'SUPERSET_USER', :'SUPERSET_PASS');
  ELSE
    EXECUTE format('ALTER ROLE %I WITH LOGIN PASSWORD %L', :'SUPERSET_USER', :'SUPERSET_PASS');
  END IF;
END $$;
```

### 5) [`sql/db/010_databases.sql`](../../../sql/db/010_databases.sql)

```sql
SELECT format('CREATE DATABASE %I OWNER %I', :'PROJECT_DB', :'PROJECT_USER')
WHERE NOT EXISTS (SELECT 1 FROM pg_database WHERE datname = :'PROJECT_DB')
\gexec

SELECT format('CREATE DATABASE %I OWNER %I', :'AIRFLOW_DB', :'AIRFLOW_USER')
WHERE NOT EXISTS (SELECT 1 FROM pg_database WHERE datname = :'AIRFLOW_DB')
\gexec

SELECT format('CREATE DATABASE %I OWNER %I', :'SUPERSET_DB', :'SUPERSET_USER')
WHERE NOT EXISTS (SELECT 1 FROM pg_database WHERE datname = :'SUPERSET_DB')
\gexec
```

### 6) [`sql/db/020_extensions.sql`](../../../sql/db/020_extensions.sql) (минимум, правь под себя)

```sql
CREATE EXTENSION IF NOT EXISTS pgcrypto;
```

### 7) [`sql/db/030_project_schemas.sql`](../../../sql/db/030_project_schemas.sql)

```sql
CREATE SCHEMA IF NOT EXISTS raw;
CREATE SCHEMA IF NOT EXISTS stg;
CREATE SCHEMA IF NOT EXISTS mart;
CREATE SCHEMA IF NOT EXISTS mon;

ALTER SCHEMA raw  OWNER TO :"PROJECT_USER";
ALTER SCHEMA stg  OWNER TO :"PROJECT_USER";
ALTER SCHEMA mart OWNER TO :"PROJECT_USER";
ALTER SCHEMA mon  OWNER TO :"PROJECT_USER";
```

### 8) [`sql/db/040_project_migrations.sql`](../../../sql/db/040_project_migrations.sql)

```sql
CREATE TABLE IF NOT EXISTS mon.applied_migrations (
  migration_key text PRIMARY KEY,
  applied_at timestamptz NOT NULL DEFAULT now()
);

ALTER TABLE mon.applied_migrations OWNER TO :"PROJECT_USER";
```

### 9) [`sql/db/050_project_baseline.sql`](../../../sql/db/050_project_baseline.sql) (минимальные demo-объекты под seed/smoke)

```sql
CREATE TABLE IF NOT EXISTS mart.demo_kpi_daily (
  dt date PRIMARY KEY,
  vacancies_cnt integer NOT NULL,
  employers_cnt integer NOT NULL
);

CREATE TABLE IF NOT EXISTS mon.demo_dag_runs_daily (
  dt date PRIMARY KEY,
  dag_runs_cnt integer NOT NULL,
  failed_cnt integer NOT NULL
);

ALTER TABLE mart.demo_kpi_daily OWNER TO :"PROJECT_USER";
ALTER TABLE mon.demo_dag_runs_daily OWNER TO :"PROJECT_USER";
```

### 10) [`sql/db/900_seed.sql`](../../../sql/db/900_seed.sql) (30 дней)

```sql
WITH d AS (
  SELECT (current_date - offs)::date AS dt
  FROM generate_series(0, 29) AS offs
)
INSERT INTO mart.demo_kpi_daily (dt, vacancies_cnt, employers_cnt)
SELECT
  dt,
  (1000 + (extract(day from dt)::int * 7))::int,
  (120 + (extract(day from dt)::int % 10))::int
FROM d
ON CONFLICT (dt) DO NOTHING;

WITH d AS (
  SELECT (current_date - offs)::date AS dt
  FROM generate_series(0, 29) AS offs
)
INSERT INTO mon.demo_dag_runs_daily (dt, dag_runs_cnt, failed_cnt)
SELECT
  dt,
  12,
  (extract(day from dt)::int % 3)
FROM d
ON CONFLICT (dt) DO NOTHING;
```

## Smoke-проверки (скрипт/команда)

### [`scripts/db-smoke.sh`](../../../scripts/db-smoke.sh)

```bash
#!/usr/bin/env bash
set -euo pipefail

: "${POSTGRES_SUPERUSER:?}"
: "${POSTGRES_SUPERPASS:?}"
: "${POSTGRES_HOST_PORT:?}"
: "${PROJECT_DB:?}"
: "${AIRFLOW_DB:?}"
: "${SUPERSET_DB:?}"

export PGPASSWORD="$POSTGRES_SUPERPASS"

psqlh() { psql -v ON_ERROR_STOP=1 -h 127.0.0.1 -p "$POSTGRES_HOST_PORT" -U "$POSTGRES_SUPERUSER" "$@"; }

echo "[smoke] check databases exist..."
cnt="$(psqlh -d postgres -Atc \
  "select count(*) from pg_database where datname in ('$PROJECT_DB','$AIRFLOW_DB','$SUPERSET_DB');")"
[[ "$cnt" == "3" ]] || { echo "expected 3 dbs, got $cnt"; exit 1; }

echo "[smoke] check schemas in project_db..."
scnt="$(psqlh -d "$PROJECT_DB" -Atc \
  "select count(*) from information_schema.schemata where schema_name in ('raw','stg','mart','mon');")"
[[ "$scnt" == "4" ]] || { echo "expected 4 schemas, got $scnt"; exit 1; }

echo "[smoke] check applied_migrations exists..."
t="$(psqlh -d "$PROJECT_DB" -Atc "select to_regclass('mon.applied_migrations') is not null;")"
[[ "$t" == "t" ]] || { echo "applied_migrations not found"; exit 1; }

echo "[smoke] check seed demo tables (may be empty if DB_SEED_ENABLED=0)..."
kpi="$(psqlh -d "$PROJECT_DB" -Atc "select count(*) from mart.demo_kpi_daily;")"
mon="$(psqlh -d "$PROJECT_DB" -Atc "select count(*) from mon.demo_dag_runs_daily;")"
echo "[smoke] mart.demo_kpi_daily rows=$kpi, mon.demo_dag_runs_daily rows=$mon"

echo "[smoke] OK"
```

(Если хочешь строго: при `DB_SEED_ENABLED=1` — требовать `rows > 0`, при `0` — не требовать.)


