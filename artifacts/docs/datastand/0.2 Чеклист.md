# Чеклист работ

Составь чек-лист всех действий, который надо по итогу нам с тобой сделать для развертывания инфраструктуры.

Дополнительная информация:

> Миграции для project_db: делаем таблицу applied_migrations (рекомендую) или идемпотентный DDL без учёта?

Давай сделаем

> src/ в Airflow: ставим в образ (чисто и предсказуемо) или mount + PYTHONPATH (быстрее для итераций)?

Попробуем поставить в образ

> Seed: минимальный (чисто чтобы графики были не пустые) или “чуть похож на жизнь” (7–30 дней динамики)?

Возьмем 30 дней

> Доп. данные, которые ты можешь дать позже (ускорят работу):
> Желаемый нейминг проекта (префикс для volumes/networks)

works on Russia

> Минимальный список таблиц для mart/mon (чтобы seed и дашборды “попали”)

Это пока рано. Таблицы дополню после развертывания инфры написания всех запросов и дагов, а затем уже добавлю в sql init

Ниже чек-лист того, что нам с тобой надо сделать до состояния “ревьюер поднял всё одной командой, увидел Airflow+Superset+Postgres, дашборды не пустые”. Да, это будет чуть более “как в проде”, чем обычно для портфолио (и это хорошо).

## 0) Зафиксированные решения (чтобы не расползалось)

- [x] Compose profiles: `db`, `db-init`, `airflow`, `superset`
- [x] Один Postgres-контейнер, внутри 3 БД: `project_db`, `airflow_db`, `superset_db`
- [x] Миграции project_db: таблица `applied_migrations`
- [x] Airflow executor: LocalExecutor
- [x] [`src/`](../../../src) ставим в образ Airflow (а не mount)
- [x] Seed: 30 дней динамики
- [x] Таймзона: Europe/Moscow (+3)
- [x] Порты по умолчанию: 5432/8080/8088, переопределяем через `.env`
- [x] Проектный префикс: `works on Russia` → в технике используем безопасный идентификатор `works_on_russia` (без пробелов)

## 1) Репозиторий и структура (скелет)

- [ ] Создать дерево директорий:
  - [x] `compose.yaml`
  - [x] `.env.example`
  - [x] `docs/infra/INFRA_SPEC.md`
  - [x] [`docs/db/README.md`](../../../docs/db/README.md)
  - [x] [`sql/db/`](../../../sql/db) (init/migrations/seed)
  - [x] [`src/`](../../../src) (ETL-код)
  - [x] [`airflow/`](../../../airflow) (Dockerfile + entrypoint + dags-обвязка)
  - [x] [`superset/`](../../../superset) (Dockerfile + bootstrap + assets)
  - [x] [`superset/assets/`](../../../superset/assets) (экспорт раздельно по сущностям)
  - [x] [`scripts/`](../../../scripts) (make/run/reset/smoke)
  - [x] [`artifacts/`](../..) (excel и прочее, в git не идёт)

- [x] Настроить `.gitignore`:
  - [x] `.env`, `__pycache__`, `.venv`, [`artifacts/`](../..), локальные volume-дампы и т.п.

- [x] Ввести единый нейминг и соглашения:
  - [x] `COMPOSE_PROJECT_NAME=works_on_russia`
  - [x] сеть: `works_on_russia_net`
  - [x] volume: `works_on_russia_pgdata` (и прочие по необходимости)

## 2) Compose базовый каркас (без сервисов ещё, но с правилами)

- [x] Описать profiles и общие элементы:
  - [x] одна bridge-сеть с фикс-именем
  - [x] named volumes (кросс-ОС стабильнее, чем bind)
  - [x] healthcheck для Postgres (обязателен)
  - [x] единые переменные TZ/локаль

- [x] Добавить “команды управления” (любой вариант):
  - [x] `Makefile` или `taskfile` или `scripts/*.sh`
  - [x] команды: `up-db`, `init-db`, `reset-soft`, `reset-hard`, `up-airflow`, `up-superset`, `up-all`, `smoke`

Ирония №1: если это не автоматизировать, ты будешь “автоматизацией” работать руками.

## 3) Модуль Postgres (делаем первым)

### 3.1 Сервис `postgres` (profile `db`)

- [x] Подключить image Postgres (tag через env, “предпоследний стабильный” зафиксировать)
- [x] Опубликовать порт наружу по умолчанию (`POSTGRES_HOST_PORT`)
- [x] Примонтировать `works_on_russia_pgdata` в `PGDATA`
- [x] Включить healthcheck (pg_isready)
- [x] Настроить логирование (хотя бы дефолтное, но читаемое)

### 3.2 Сервис `db-init` one-shot (profile `db-init`)

- [ ] Реализовать “ждём Postgres → применяем скрипты по порядку → выходим 0”
- [ ] Жёсткий порядок применения SQL:
  - [ ] `000_roles.sql` (создание ролей/пользователей)
  - [ ] `010_databases.sql` (создание 3 БД)
  - [ ] `020_extensions.sql` (минимальный набор)
  - [ ] `030_project_schemas.sql` (raw/stg/mart/mon в `project_db`)
  - [ ] `040_project_migrations.sql` (создать `applied_migrations`)
  - [ ] `050_project_baseline.sql` (базовые объекты проекта, если есть)
  - [ ] `900_seed.sql` (демо данные)

- [ ] Сделать `applied_migrations` и механизм применения миграций:
  - [ ] правило: имя файла миграции = ключ миграции
  - [ ] транзакционность (по возможности)
  - [ ] запись в `applied_migrations` только при успешном применении

- [ ] Сделать smoke-проверки (скрипт/команда):
  - [ ] существуют 3 БД
  - [ ] в `project_db` есть схемы raw/stg/mart/mon
  - [ ] есть `applied_migrations`
  - [ ] seed (если включён) загрузился

### 3.3 Reset-сценарии

- [ ] Hard reset:
  - [ ] `docker compose down -v` (или целевой volume rm) документирован и безопасен

- [ ] Soft reset (без удаления datadir):
  - [ ] скрипт: дроп/очистка схем raw/stg/mart/mon в `project_db`
  - [ ] повторный прогон seed
  - [ ] не трогать роли, не трогать сами БД `airflow_db/superset_db`

Ирония №2: reset без спецификации — это когда “починилось” только у того, кто чинил.

## 4) Модуль Airflow

### 4.1 Образ Airflow (ставим [`src/`](../../../src) в image)

- [ ] Dockerfile:
  - [ ] базовый образ Airflow зафиксирован (tag через env)
  - [ ] установить зависимости (лучше lock-файл)
  - [ ] скопировать [`src/`](../../../src) внутрь образа
  - [ ] выставить `PYTHONPATH` так, чтобы DAG-и импортировали из `src`

- [ ] Стратегия итераций:
  - [ ] на разработке допускаем rebuild при изменениях в `src` (да, медленнее)
  - [ ] DAG-файлы можно либо тоже “в образ”, либо mount (быстрее править), но тогда строго: DAG-и тонкие

### 4.2 Compose-сервисы Airflow (profile `airflow`)

- [ ] `airflow-init` one-shot:
  - [ ] миграции metadata в `airflow_db`
  - [ ] создать admin пользователя (креды в `.env.example` как пример)

- [ ] `airflow-webserver`
- [ ] `airflow-scheduler`
- [ ] Настроить LocalExecutor
- [ ] Настроить timezone Europe/Moscow
- [ ] Подключение к Postgres:
  - [ ] DSN к `airflow_db`
  - [ ] DSN к `project_db` (для задач)

- [ ] Том(а) для логов (persist) и, при необходимости, для plugins

### 4.3 Runbook и smoke

- [ ] Команда: поднять только airflow-профиль (при поднятом db)
- [ ] Smoke:
  - [ ] UI открывается на 8080
  - [ ] scheduler жив
  - [ ] можно сделать test connection к `project_db`

Ирония №3: Airflow “встал” — не значит, что он “работает”. Он просто научился дышать.

## 5) Модуль Superset

### 5.1 Образ Superset и bootstrap

- [ ] Dockerfile/entrypoint:
  - [ ] миграции Superset в `superset_db`
  - [ ] создать admin (пример кредов в `.env.example`)
  - [ ] создать подключение к `project_db` (конфиг через env)
  - [ ] импортировать assets из [`superset/assets/`](../../../superset/assets) автоматически

## 5.2 Контракт assets (раздельно по сущностям)

- [ ] Определить, какие сущности храним:
  - [ ] dashboards
  - [ ] charts
  - [ ] datasets
  - [ ] (database connection не хардкодим в asset-файлах; создаём через bootstrap/env)

- [ ] Команда экспорт/импорт документирована
- [ ] После `up superset` дашборды видны без ручных кликов “собери сам”

## 6) Seed и “дашборд из коробки”

Тут важная развилка: ты сказал “таблицы пока рано”, но “дашборд из коробки без API” требует хотя бы минимальных таблиц/datasets.

Минимальный компромисс (рекомендую):

- [ ] Сделать временный минимальный набор таблиц **только для демо** (mart/mon), чтобы:
  - [ ] Superset мог построить графики
  - [ ] seed мог насыпать 30 дней

- [ ] Позже заменить на реальные таблицы, но интерфейс дашборда сохранить

Ирония №4: “таблицы потом” и “дашборд сейчас” одновременно живут только в презентациях.

## 7) Безопасность и конфиг (чтобы ревьюер не ругался)

- [ ] `.env` не коммитим, только `.env.example`
- [ ] В `.env.example` креды как пример, но явно помечены “локально”
- [ ] Superset `SECRET_KEY` задан через env
- [ ] Порты и креды переопределяемы через env
- [ ] Никаких “магических” значений, зависящих от ОС/пути/имени папки

## 8) Документация (то, чем будем руководствоваться)

- [ ] README (инфра-спек скелет + команды)
- [ ] `docs/infra/INFRA_SPEC.md` (развёрнутый документ)
- [ ] [`docs/db/README.md`](../../../docs/db/README.md) (контракт db, init/reset/smoke)
- [ ] “Known issues / troubleshooting” (минимум топ-5)

## 9) Мини-CI (по желанию, но сильно ускоряет)

- [ ] Проверка `docker compose config` (валидность)
- [ ] Линт/проверка, что `.env` не попал в git
- [ ] Smoke job: поднять `db` + `db-init` и прогнать проверки (без airflow/superset на первом шаге)
